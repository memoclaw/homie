app:
  logLevel: info
  dataDir: ./data

# Telegram (required)
telegram:
  botToken: ${TELEGRAM_BOT_TOKEN}
  allowedChatIds: []

# Claude Code provider
provider:
  kind: codex
  model: "gpt-5.4"
  extraArgs: []
