{
  "name": "homie",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/channels/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "bun run apps/server/src/index.ts",
    "cli": "bun run apps/cli/src/index.ts",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "test": "bun test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.6",
    "@types/bun": "^1.3.10",
    "typescript": "^5.9.3"
  }
}
